name: Build APK

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

    # Checkout your code
    - name: Checkout code
      uses: actions/checkout@v3

    # Install system packages
    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y unzip zip git openjdk-17-jdk python3-pip
        pip install --upgrade pip
        pip install cython buildozer

    # Create SDK & NDK directories
    - name: Prepare Android SDK & NDK folders
      run: |
        mkdir -p $HOME/android/sdk
        mkdir -p $HOME/android/ndk

    # Install Android NDK r25b
    - name: Install Android NDK (r25b)
      run: |
        wget https://dl.google.com/android/repository/android-ndk-r25b-linux.zip -O ndk.zip
        unzip ndk.zip -d $HOME/android/ndk

    # Install Android commandline tools (safe version)
    - name: Install Android commandline tools
      run: |
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O cmd.zip
        mkdir -p $HOME/android/sdk/cmdline-tools
        unzip cmd.zip -d $HOME/android/sdk/cmdline-tools
        mv $HOME/android/sdk/cmdline-tools/cmdline-tools $HOME/android/sdk/cmdline-tools/latest

    # Install platform tools + build-tools 33.0.2
    - name: Install Android packages
      run: |
        yes | $HOME/android/sdk/cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android/sdk \
          "platform-tools" \
          "platforms;android-33" \
          "build-tools;33.0.2"

    # Export environment variables (Buildozer needs these)
    - name: Set environment variables
      run: |
        echo "ANDROIDSDK=$HOME/android/sdk" >> $GITHUB_ENV
        echo "ANDROIDNDK=$HOME/android/ndk/android-ndk-r25b" >> $GITHUB_ENV
        echo "PATH=$HOME/android/sdk/platform-tools:$HOME/android/sdk/cmdline-tools/latest/bin:$PATH" >> $GITHUB_ENV

    # Build the APK
    - name: Build APK
      run: buildozer android debug

    # Upload resulting APK
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: safespot-apk
        path: bin/*.apk
